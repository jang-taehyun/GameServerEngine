syntax = "proto3";
package Protocol;

import "Enum.proto";
import "Struct.proto";

message C_TEST
{

}

message S_TEST
{
	uint64 ID = 1;
	uint32 HP = 2;
	uint32 attack = 3;
	repeated BuffData buffs = 4;
}

message S_LOGIN
{

}

// protoBuf를 C++로 사용할 때,
// string은 UTF-8 인코딩을 사용한다.
// 때문에 UTF-16 인코딩을 사용할 경우, BTYE 배열로 변환해서 보내고,
// 받는 쪽에서는 BYTE 배열을 wstring 형식으로 변환해야 한다.